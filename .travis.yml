language: node_js
node_js:
- node
cache:
  directories:
    - node_modules
script: yarn install
after_success:
- yarn build:ghp
deploy:
  - provider: pages
    skip_cleanup: true
    local-dir: dist
    github-token: $GITHUB_TOKEN
    on:
      branch: master
